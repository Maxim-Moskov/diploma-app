services:
  web:
    image: ${APP_IMAGE:-moskovmaxim/diploma-app:latest}
    container_name: diploma-app
    restart: unless-stopped
    ports:
      - "80:80"
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost/"]
      interval: 30s
      timeout: 3s
      retries: 3
